FROM postgres:17

# Add AWS cli
RUN apt-get update; \
    apt-get install -y --no-install-recommends \
    awscli

# Add the backup script
COPY ./database-backup.sh /backup.sh
RUN chmod +x /backup.sh

ENTRYPOINT [ "/backup.sh" ]